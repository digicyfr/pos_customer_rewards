<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Extend POS Config Form View with Loyalty Program -->
    <record id="pos_config_view_form_loyalty" model="ir.ui.view">
        <field name="name">pos.config.form.loyalty</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="inside">
                <group string="Customer Rewards" name="loyalty_group">
                    <field name="company_id" invisible="1"/>
                    <field name="loyalty_program_id"/>
                    <field name="loyalty_enabled" invisible="1"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
